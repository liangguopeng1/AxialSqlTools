<Window x:Class="AxialSqlTools.IndexBuildProgressWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="索引构建"
        WindowStyle="None"
        AllowsTransparency="True"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        ShowActivated="False"
        Width="340"
        Height="118"
        Background="Transparent"
        Focusable="False">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/AxialSqlTools;component/Themes/SharedToolWindowTheme.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <Border Background="{DynamicResource AxialThemeBackgroundBrush}"
            BorderBrush="{DynamicResource AxialThemeBorderBrush}"
            BorderThickness="1"
            CornerRadius="6"
            Padding="12,10">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <TextBlock x:Name="TitleText"
                       Grid.Row="0"
                       FontWeight="SemiBold"
                       Text="加载 IntelliSense 缓存"
                       TextWrapping="Wrap"/>
            <ProgressBar x:Name="Bar"
                         Grid.Row="1"
                         Height="14"
                         Margin="0,8,0,6"
                         Minimum="0"
                         Maximum="100"
                         Value="0"/>
            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock x:Name="StatusText"
                           VerticalAlignment="Center"
                           Opacity="0.85"
                           FontSize="11"
                           TextWrapping="NoWrap"
                           TextTrimming="CharacterEllipsis"
                           Text="准备中..."/>
                <Button x:Name="CancelButton"
                        Grid.Column="1"
                        Margin="8,0,0,0"
                        Padding="10,2"
                        Content="取消"
                        Click="CancelButton_Click"/>
            </Grid>
        </Grid>
    </Border>
</Window>
