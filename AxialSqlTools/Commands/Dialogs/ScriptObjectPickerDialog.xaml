<Window x:Class="AxialSqlTools.ScriptObjectPickerDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Select Object"
        Tag="loc:ScriptObject_DialogTitle"
        Height="320"
        Width="800"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock Name="HeaderTextBlock" FontWeight="Bold" FontSize="14" />
        <ListBox Name="ObjectsListBox"
                 Grid.Row="1"
                 Margin="0,10,0,10"
                 DisplayMemberPath="DisplayName" />
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Select" Tag="loc:Common_Select" Width="90" Click="SelectButton_Click" />
            <Button Content="Cancel" Tag="loc:Common_Cancel" Width="90" Margin="8,0,0,0" Click="CancelButton_Click" />
        </StackPanel>
    </Grid>
</Window>
