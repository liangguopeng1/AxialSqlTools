<UserControl x:Class="AxialSqlTools.QuickSearchWindowControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:avalonedit="http://icsharpcode.net/sharpdevelop/avalonedit"
             mc:Ignorable="d"
             Name="QuickSearchControl"
             Background="{DynamicResource AxialThemeBackgroundBrush}"
             Foreground="{DynamicResource AxialThemeForegroundBrush}">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/AxialSqlTools;component/Themes/SharedToolWindowTheme.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid Background="{DynamicResource AxialThemeBackgroundBrush}">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Background="{DynamicResource AxialThemeHeaderBackgroundBrush}">
            <TextBlock Margin="5" HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="Bold"
                       FontSize="18" Tag="loc:QuickSearch_HeaderTitle">Search for Object Definitions Across Multiple Databases</TextBlock>
            <TextBlock x:Name="WikiDescriptionTextBlock" Margin="10" HorizontalAlignment="Left" VerticalAlignment="Center">
                <Run Text="Feature description in"/>
                <Run Text=" "/>
                <Hyperlink NavigateUri="https://github.com/liangguopeng1/AxialSqlTools/wiki/Quick-Search"
                           RequestNavigate="WikiLink_RequestNavigate">
                    <Run Text="Wiki"/>
                </Hyperlink>
            </TextBlock>
        </StackPanel>

        <Grid Grid.Row="1" Margin="5">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>       
        
            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="4,4,0,8">
            
                <Button Height="30"
                        Width="220"
                        Content="Select Target from Object Explorer"
                        Tag="loc:QuickSearch_SelectTarget"
                        FontWeight="Bold"
                        Click="Button_SelectConnection_Click" />
                <Label x:Name="Label_ConnectionDescription" Margin="8,0,0,0" VerticalAlignment="Center" Content="No connection selected"/>
            </StackPanel>

            <Grid Grid.Row="1" x:Name="SearchInputsGrid" Margin="0,0,0,8" IsEnabled="False">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Label Grid.Column="0" Content="Search for:" Tag="loc:QuickSearch_SearchFor" VerticalAlignment="Center"/>
                <TextBox Grid.Column="1" x:Name="TextBox_SearchText" Margin="6,0,12,0" Height="24" VerticalContentAlignment="Center" KeyDown="TextBox_SearchText_KeyDown"/>
                <CheckBox Grid.Column="2" x:Name="CheckBox_AllDatabases" Content="All user databases on the server" Tag="loc:QuickSearch_AllDatabases" VerticalAlignment="Center" Margin="0,0,12,0" IsChecked="True"/>
                <StackPanel Grid.Column="3" Orientation="Horizontal" VerticalAlignment="Center">
                    <CheckBox x:Name="CheckBox_WholeWord" Content="Match whole words only" Tag="loc:QuickSearch_WholeWord" Margin="0,0,12,0" Checked="CheckBox_WholeWord_Checked"/>
                    <CheckBox x:Name="CheckBox_UseWildcards" Content="Use wildcards" Tag="loc:QuickSearch_Wildcards" Checked="CheckBox_UseWildcards_Checked" />
                </StackPanel>
                <Button Grid.Column="4" x:Name="Button_Search" Content="Search" Tag="loc:Common_Search" Width="90" Height="28" Click="Button_Search_Click" Style="{DynamicResource AxialPrimaryButtonStyle}"/>
            </Grid>

            <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,8">
                <Label Content="Object types:" Tag="loc:QuickSearch_ObjectTypes" VerticalAlignment="Center"/>
                <CheckBox x:Name="CheckBox_StoredProcedures" Content="Stored Procedures" Tag="loc:QuickSearch_StoredProcedures" IsChecked="True" Margin="8,0,0,0" VerticalAlignment="Center"/>
                <CheckBox x:Name="CheckBox_Views" Content="Views" Tag="loc:QuickSearch_Views" IsChecked="True" Margin="8,0,0,0" VerticalAlignment="Center"/>
                <CheckBox x:Name="CheckBox_Functions" Content="Functions" Tag="loc:QuickSearch_Functions" IsChecked="True" Margin="8,0,0,0" VerticalAlignment="Center"/>
                <CheckBox x:Name="CheckBox_Tables" Content="Tables" Tag="loc:QuickSearch_Tables" IsChecked="True" Margin="8,0,0,0" VerticalAlignment="Center"/>
                <CheckBox x:Name="CheckBox_AgentJobSteps" Content="SQL Agent Jobs" Tag="loc:QuickSearch_AgentJobs" IsChecked="True" Margin="8,0,0,0" VerticalAlignment="Center"/>
            </StackPanel>

            <DataGrid Grid.Row="3"
                      x:Name="DataGrid_SearchResults"
                      Style="{DynamicResource AxialThemedDataGridStyle}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      SelectionMode="Single"
                      SelectionChanged="DataGrid_SearchResults_SelectionChanged">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Database" Binding="{Binding DatabaseName}" Width="130"/>
                    <DataGridTextColumn Header="Type" Binding="{Binding ObjectType}" Width="140"/>
                    <DataGridTextColumn Header="Schema" Binding="{Binding SchemaName}" Width="120"/>
                    <DataGridTextColumn Header="Object" Binding="{Binding ObjectName}" Width="200"/>
                    <DataGridTextColumn Header="Location" Binding="{Binding MatchLocation}" Width="120"/>
                    <DataGridTextColumn Header="Match Preview" Binding="{Binding MatchPreview}" Width="*" FontFamily="Cascadia Mono"/>
                    <DataGridTemplateColumn Header="Script" Width="90">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="Script" Tag="loc:Common_Script" Click="Button_ScriptResult_Click" Padding="8,2"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>

            <avalonedit:TextEditor  Grid.Row="4"
                x:Name="SqlEditor"
                ShowLineNumbers="True"
                FontFamily= "Consolas"
                FontSize="13"
                Background="{DynamicResource AxialThemeBackgroundBrush}"
                Foreground="{DynamicResource AxialThemeForegroundBrush}"
                IsReadOnly="True" />

            <StatusBar Grid.Row="5"
                       Background="{DynamicResource AxialThemeBackgroundBrush}"
                       Foreground="{DynamicResource AxialThemeForegroundBrush}">
                <StatusBarItem>
                    <TextBlock x:Name="TextBlock_ResultCount" FontWeight="Bold"/>
                </StatusBarItem>
            </StatusBar>
        </Grid>
    </Grid>
</UserControl>
